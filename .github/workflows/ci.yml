name: trident tests

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  fast-unit-tests:
    name: Fast Unit Tests (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.11" ]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install apt dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libvips libvips-dev openslide-tools

      - name: Install python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Run fast unit tests only
        env:
          TRIDENT_RUN_INTEGRATION_TESTS: "0"
          TRIDENT_RUN_GPU_TESTS: "0"
          TRIDENT_ALLOW_HF_WEIGHTS_DOWNLOAD: "1"
        run: |
          python -m unittest discover -s tests -p "test_*.py" -v